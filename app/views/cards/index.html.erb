<div class="well">
  <h3>贺卡</h3>
</div>
<table class="table table-striped">
  <thead>
    <tr>
    <th>订单号</th>
    <th>内容</th>
    <th>图片</th>
    <th>url</th>
  </tr>
  </thead>
  <tbody>
  <% @cards.each do |card| %>
    <tr>
      <td><a href="/cards/<%=Base64.encode64(card.order_no).gsub('\n','')%>"><%= card.order_no%></a>
        <a href="/cards/show_code?id=<%=card.id%>">二维码</a></td>
      <td><%= card.content %></td>
       <td><a href="<%= card.card_image.picture_file_name %>"><%= card.card_image.picture_file_name %></a></td>
       <td><%=card.url %></td>
    </tr>

  <% end %>
  </tbody>
</table>
